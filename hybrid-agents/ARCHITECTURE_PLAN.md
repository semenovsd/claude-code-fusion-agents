# Архитектурный план гибридной системы субагентов

## 🎯 Цель: Создать интеллектуальную, адаптивную систему оркестрации

---

## 1. Архитектура системы

### 1.1 Иерархическая структура

```
┌─────────────────────────────────────────────────────────┐
│              Entry Point: User Request                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│         Level 1: Task Router (task-router)              │
│  - Analyzes request complexity                          │
│  - Determines workflow type                             │
│  - Routes to appropriate orchestrator                   │
│  Model: haiku (lightweight analysis)                    │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌──────────────┐         ┌──────────────────┐
│   Simple     │         │   Complex        │
│   Route      │         │   Route          │
└──────┬───────┘         └────────┬─────────┘
       │                          │
       │                          │
       ▼                          ▼
┌──────────────┐         ┌──────────────────┐
│   Direct     │         │  Workflow        │
│   Agent      │         │  Orchestrator    │
│   Execution  │         │  (Full System)   │
└──────────────┘         └────────┬─────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
            ┌──────────┐  ┌──────────┐  ┌──────────┐
            │Planning  │  │Development│  │Validation│
            │Phase     │  │Phase      │  │Phase     │
            └──────────┘  └──────────┘  └──────────┘
```

### 1.2 Компоненты системы

#### Core Agents (Уровень 1-2)
1. **task-router** - Определение сложности и маршрутизация
2. **simple-coordinator** - Легкая координация для средних задач
3. **workflow-orchestrator** - Полная оркестрация для сложных задач

#### Planning Agents (Уровень 3, только для сложных задач)
4. **task-analyzer** - Анализ задачи и определение требований
5. **requirements-clarifier** - Уточнение требований через вопросы
6. **tech-stack-detector** - Автоматическое определение стека
7. **plan-generator** - Создание детального плана

#### Development Agents (Уровень 4)
8. **frontend-developer** - Frontend разработка
9. **backend-architect** - Backend архитектура и разработка
10. **full-stack-developer** - Полноценная разработка
11. **python-pro** - Python специалист
12. **typescript-pro** - TypeScript специалист
13. **golang-pro** - Go специалист
14. И другие специализированные агенты...

#### Quality Agents (Уровень 4-5)
15. **code-reviewer** - Код ревью
16. **test-automator** - Автоматизация тестов
17. **security-auditor** - Безопасность
18. **performance-engineer** - Производительность

#### Validation Agents (Уровень 5, только для сложных задач)
19. **quality-gate-checker** - Проверка quality gates
20. **result-validator** - Валидация результатов
21. **iteration-manager** - Управление итерациями

---

## 2. Алгоритм определения сложности

### 2.1 Критерии сложности

```python
def determine_complexity(request: str, context: dict) -> str:
    """
    Определяет сложность задачи на основе:
    1. Размер задачи (количество файлов, строк кода)
    2. Количество технологий (frontend + backend + database)
    3. Неопределенности (вопросы, неясные требования)
    4. Тип задачи (bug fix, feature, refactoring, new service)
    """
    
    score = 0
    
    # Размер задачи
    if "single file" in request or "one method" in request:
        score += 1
    elif "component" in request or "feature" in request:
        score += 3
    elif "service" in request or "system" in request:
        score += 5
    
    # Количество технологий
    tech_count = detect_technologies(context)
    score += min(tech_count, 3)
    
    # Неопределенности
    if "?" in request or "clarify" in request.lower():
        score += 2
    
    # Тип задачи
    if "bug fix" in request.lower():
        score += 1
    elif "refactor" in request.lower():
        score += 2
    elif "new" in request.lower() and "service" in request.lower():
        score += 5
    
    # Определение уровня
    if score <= 2:
        return "simple"
    elif score <= 5:
        return "medium"
    elif score <= 8:
        return "complex"
    else:
        return "enterprise"
```

### 2.2 Маршрутизация по сложности

| Сложность | Workflow | Оркестратор | Модель | Агентов | Токены |
|-----------|----------|-------------|--------|---------|--------|
| Simple | Direct | Нет | haiku/sonnet | 1 | 10-30K |
| Medium | Light | simple-coordinator | haiku | 2-3 | 30-100K |
| Complex | Full | workflow-orchestrator | sonnet | 5-7 | 100-500K |
| Enterprise | Multi-phase | workflow-orchestrator | sonnet | 7+ | 500K+ |

---

## 3. Workflow паттерны

### 3.1 Simple Workflow

```
User Request
    ↓
task-router (haiku, ~5K tokens)
    ↓
[Complexity: Simple]
    ↓
Direct Agent Execution
    ↓
Result
```

**Характеристики**:
- Время: 1-3 минуты
- Токены: 10-30K
- Агентов: 1
- Quality gates: Нет

### 3.2 Medium Workflow

```
User Request
    ↓
task-router (haiku, ~5K tokens)
    ↓
[Complexity: Medium]
    ↓
simple-coordinator (haiku, ~10K tokens)
    ├─ Analyzes task
    ├─ Selects 2-3 agents
    └─ Coordinates execution
    ↓
Agent Team (2-3 agents, parallel/sequential)
    ↓
Result Aggregation
    ↓
Result
```

**Характеристики**:
- Время: 5-15 минут
- Токены: 30-100K
- Агентов: 2-3
- Quality gates: Базовые проверки

### 3.3 Complex Workflow

```
User Request
    ↓
task-router (haiku, ~5K tokens)
    ↓
[Complexity: Complex]
    ↓
workflow-orchestrator (sonnet, ~20K tokens)
    ↓
┌─────────────────────────────────────┐
│ Phase 1: Planning                  │
│ ├─ task-analyzer                   │
│ ├─ requirements-clarifier          │
│ ├─ tech-stack-detector             │
│ └─ plan-generator                  │
└─────────────┬───────────────────────┘
              │
              ▼
    ┌─────────────────────┐
    │ Quality Gate 1      │
    │ (planning complete)  │
    └─────────┬────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ Phase 2: Development                │
│ ├─ backend-architect                │
│ ├─ frontend-developer               │
│ ├─ test-automator                   │
│ └─ code-reviewer                   │
└─────────────┬───────────────────────┘
              │
              ▼
    ┌─────────────────────┐
    │ Quality Gate 2      │
    │ (development ok)    │
    └─────────┬────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ Phase 3: Validation                │
│ ├─ result-validator                 │
│ ├─ security-auditor                │
│ └─ quality-gate-checker            │
└─────────────┬───────────────────────┘
              │
              ▼
    ┌─────────────────────┐
    │ Quality Gate 3      │
    │ (production ready)  │
    └─────────┬────────────┘
              │
              ▼
         Result
```

**Характеристики**:
- Время: 30-120 минут
- Токены: 100-500K
- Агентов: 5-7
- Quality gates: Строгие проверки на каждом этапе
- Итерации: Возможны при провале quality gates

### 3.4 Enterprise Workflow

```
User Request
    ↓
task-router (haiku, ~5K tokens)
    ↓
[Complexity: Enterprise]
    ↓
workflow-orchestrator (sonnet, ~30K tokens)
    ↓
┌─────────────────────────────────────┐
│ Phase 1: Planning & Analysis       │
│ ├─ task-analyzer (iterative)       │
│ ├─ requirements-clarifier          │
│ ├─ tech-stack-detector             │
│ ├─ plan-generator                  │
│ └─ architecture-reviewer           │
└─────────────┬───────────────────────┘
              │
              ▼
    ┌─────────────────────┐
    │ Quality Gate 1     │
    │ (strict: >95%)      │
    └─────────┬────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ Phase 2: Development (Parallel)   │
│ ├─ backend-architect               │
│ ├─ frontend-developer              │
│ ├─ database-optimizer              │
│ ├─ test-automator                  │
│ └─ api-documenter                  │
└─────────────┬───────────────────────┘
              │
              ▼
    ┌─────────────────────┐
    │ Quality Gate 2     │
    │ (strict: >85%)      │
    └─────────┬────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ Phase 3: Integration & Testing     │
│ ├─ integration-tester              │
│ ├─ performance-engineer            │
│ └─ security-auditor                │
└─────────────┬───────────────────────┘
              │
              ▼
    ┌─────────────────────┐
    │ Quality Gate 3     │
    │ (strict: >90%)      │
    └─────────┬────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ Phase 4: Review & Optimization     │
│ ├─ code-reviewer                   │
│ ├─ architect-reviewer              │
│ └─ result-validator                │
└─────────────┬───────────────────────┘
              │
              ▼
    ┌─────────────────────┐
    │ Quality Gate 4     │
    │ (strict: >95%)      │
    └─────────┬────────────┘
              │
              ▼
         Result
```

**Характеристики**:
- Время: 2+ часа
- Токены: 500K+
- Агентов: 7+
- Quality gates: Строгие проверки (>95% для критических этапов)
- Итерации: Обязательны при провале gates
- Параллелизм: Максимальное использование параллельного выполнения

---

## 4. Quality Gates система

### 4.1 Критерии quality gates

#### Gate 1: Planning Phase
- Requirements completeness: >95%
- Architecture feasibility: Validated
- Task breakdown: Granular enough
- Risk assessment: Complete

#### Gate 2: Development Phase
- Code quality: >85%
- Test coverage: >80%
- Performance: Benchmarks met
- Security: No critical vulnerabilities

#### Gate 3: Validation Phase
- Code review: Approved
- All tests: Passing
- Documentation: Complete
- Deployment: Ready

#### Gate 4: Enterprise Phase (дополнительно)
- Architecture review: Approved
- Integration tests: Passing
- Performance: Optimized
- Security audit: Passed

### 4.2 Обработка провала gates

```python
def handle_gate_failure(gate_number: int, failures: list):
    """
    При провале quality gate:
    1. Анализирует причины провала
    2. Создает задачи для исправления
    3. Назначает соответствующих агентов
    4. Повторяет проверку после исправлений
    """
    if gate_number == 1:
        # Вернуться к planning phase
        return "restart_planning"
    elif gate_number == 2:
        # Исправить проблемы в development
        return "fix_and_retry"
    elif gate_number == 3:
        # Улучшить validation
        return "improve_and_retry"
```

---

## 5. Оптимизация токенов

### 5.1 Стратегии оптимизации

1. **Выбор модели по сложности**:
   - haiku для простых задач (экономия 60-70%)
   - sonnet для сложных задач (качество)

2. **Кэширование контекста**:
   - Анализ проекта кэшируется
   - Переиспользование между агентами
   - Экономия 20-30% токенов

3. **Компактные промпты**:
   - Целевой размер: 120-180 строк
   - Убрать избыточность
   - Фокус на ключевых принципах

4. **Параллельное выполнение**:
   - Независимые задачи параллельно
   - Экономия времени и токенов

### 5.2 Оценка токенов

| Компонент | Simple | Medium | Complex | Enterprise |
|-----------|--------|--------|---------|------------|
| task-router | 5K | 5K | 5K | 5K |
| Orchestrator | 0 | 10K | 20K | 30K |
| Planning | 0 | 0 | 30K | 50K |
| Development | 20K | 60K | 200K | 300K |
| Quality | 0 | 10K | 50K | 80K |
| Validation | 0 | 0 | 30K | 50K |
| **Total** | **25K** | **85K** | **335K** | **515K** |

---

## 6. Интеллектуальное планирование

### 6.1 Процесс планирования

```
1. task-analyzer:
   - Анализирует задачу
   - Определяет требования
   - Выявляет неопределенности
   
2. requirements-clarifier:
   - Задает уточняющие вопросы
   - Получает ответы от пользователя
   - Формирует четкие требования
   
3. tech-stack-detector:
   - Анализирует проект
   - Определяет технологии
   - Предлагает стек если нужно
   
4. plan-generator:
   - Создает детальный план
   - Разбивает на подзадачи
   - Определяет зависимости
   - Оценивает ресурсы
```

### 6.2 Выходные артефакты планирования

- `requirements.md` - Детальные требования
- `architecture.md` - Архитектура решения
- `plan.md` - Детальный план с задачами
- `tech-stack.md` - Технологический стек
- `risks.md` - Риски и митигация

---

## 7. Интеграция компонентов

### 7.1 Протоколы коммуникации

```json
{
  "from": "task-router",
  "to": "workflow-orchestrator",
  "type": "task_assignment",
  "payload": {
    "complexity": "complex",
    "task": "...",
    "context": {...},
    "estimated_tokens": 335000,
    "estimated_time": "90 minutes"
  }
}
```

### 7.2 Управление состоянием

```json
{
  "workflow_id": "wf_123",
  "status": "development",
  "phase": 2,
  "agents": [
    {"name": "backend-architect", "status": "completed"},
    {"name": "frontend-developer", "status": "in_progress"}
  ],
  "quality_gates": [
    {"gate": 1, "status": "passed", "score": 96},
    {"gate": 2, "status": "pending"}
  ],
  "tokens_used": 150000,
  "tokens_estimated": 335000
}
```

---

## 8. Следующие шаги реализации

1. ✅ Создать task-router
2. ✅ Создать simple-coordinator
3. ✅ Создать workflow-orchestrator
4. ✅ Создать planning agents
5. ✅ Создать development agents (оптимизированные)
6. ✅ Создать quality agents
7. ✅ Создать validation agents
8. ✅ Настроить quality gates
9. ✅ Протестировать систему
10. ✅ Написать документацию
